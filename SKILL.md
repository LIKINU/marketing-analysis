---
name: marketing-analysis
agent_created: true
规范依赖: AI协作总规范 §2.1 硬规则 · §2.4 写文档前三问 Gate · §2.6 语言约定 · §4.8 质量工程
description: Enterprise marketing & business-model breakdown to MBB/4A delivery standards. Give it any company name and it produces a logical, data-backed analysis report (Markdown + Word): issue tree & hypothesis ledger, business-model deconstruction, STP/4P/play identification, AI-marketing deep dive, AI-vs-traditional ops, Red-Team pressure test, strengths & opportunities, AIGC bonus. Triggers (EN): "analyze company X", "break down X's marketing", "business model analysis", "benchmark top-tier consulting". Triggers (中文)：「分析 XX 公司」「拆解 XX 的营销」「商业模式分析」「全量分析」「对标顶级咨询」。本 skill 是分析师视角的拆解与解读，不写执行方案。
---

# Marketing Analysis · 顶级咨询级企业全量分析

## 定位与边界

**输入**：一家企业（+ 可选侧重）。**输出**：一份对标 MBB/4A 标准的分析报告，Markdown + Word 双格式。

⚠️ **边界**：本 skill 只做「分析与解读」，不输出执行方案（排期/预算分配/行动清单）。

> **跨工具接入**：要在别的平台/Agent 上用，读 `AGENTS.md`（各平台怎么放置与触发、环境能力对照、hook 配置模板）；**派 subagent 时先给它 `AGENT-BRIEF.md`**（自动生成的单文件快照，读 1 份 ≈ 读整个仓库）。

**诚实声明（不镀金）**：本 skill 能做到顶级所的**结构、纪律、可判定标准**；做不到顶级所的**判断力、一手信息、责任承担**。

| 顶级所卖的东西 | 本 skill | 替代方案 |
|---|---|---|
| 结构化文本 + 流程纪律 | ✅ 能做，且是机械强制 | **9 支护栏脚本**（6 判据 + md2docx 交付 + 2 支回归），不过关不出稿 |
| 判断力（信息不全时押方向） | ❌ 只能做到「假设显式化 + 标明证伪条件」 | 议题树 + 假设台账 |
| 一手信息（专家网络/专有数据库） | ❌ 只有二手公开信息 | 换成「可验证的检索流程」：查什么/去哪查/查不到怎么写 |
| 资源与执行 | ❌ 只产文档 | 明确不承诺 |

**不追清单（市场上有、本 skill 不做，且不打算做）**——理由均为能力或商业边界，不是「暂时没做」：

| 市面产品有 | 本 skill | 为什么不追 |
|---|---|---|
| 素材批量生成（图/视频/数字人） | ❌ 只出文字报告 + 图表 + 物料**规范** | 需算力与版权/合规体系；另见 `references/16-物料规范与多模态交付.md` |
| 平台数据接入（抖音/腾讯/小红书 API） | ❌ 只用公开来源 | 需商务授权与广告资质 |
| 投放执行（出价/托管/程序化） | ❌ 不碰投放 | 需广告账户与资质，且属「执行」非「分析」 |
| 实时监测 / 常驻看板 | ❌ 离线一次性交付 | 需 7×24 服务与数据管道 |
| 自建大模型 | ❌ 不做模型 | 成本与产出不成比例；本 skill 走「借最强外部能力 + 自己定判据」 |

## 第 0 步 · 门禁（问完才动手）

| # | 问题 | 默认（不答时） |
|---|------|---------------|
| 1 | 分析哪家？业务范围与主要市场？ | 必答 |
| 2 | 报告体裁（全量分析 / 对标研究 / 尽调式拆解）？ | 全量分析 |
| 3 | 用途与读者（课程作业 / 内部研究 / 对外发布）？ | 课程作业 |
| 4 | 目标字数、页数上限？ | 6,000–10,000 字 |
| 5 | 侧重（均衡 / 偏 AI 营销 / 偏商业模式）？ | 均衡 |
| 6 | 要 AIGC 加分项吗（物料样稿 + 数据图表）？ | 要 |
| 7 | 交付语言？ | 简体 |
| 8 | **是否引用外部文件**（被引方案/底稿/上版报告）？若引用，请给出文件路径 | 无 |

> 第 8 项是新增的：**引用外部文件时必须拿到文件本身**，因为「引用了被引文件里不存在的东西」已被国际同行用真金白银惩罚过两次（德勤退款、毕马威撤稿），也是本 skill 真实踩过的坑。

问完产出《任务规则表》（门禁答案 + 结构 + 红线）**等确认**。确认前不出正文。
**P0 铁律**：用户给了模板/大纲/作业原文 → 标题**逐字沿用**，优先级高于本文默认骨架；模板没有的另起章节补，不改造原标题；涉及结构时**第一版只交一页故事线**；「脚本全绿」≠「交付达成」，用户验收项须另行逐条核对。

## 责任边界与签字（对标顶级所的 5 条 AI 交付红线）

> 来源：国际同行公开做法（麦肯锡/BCG/贝恩/WPP/电通等）＋两次真实处罚案例。**AI 可以写，不能负责。**

| # | 红线 | 本 skill 怎么做 |
|---|---|---|
| 1 | **署名担责不可让渡** | 报告头部必须写**署名与日期**；结论的责任由人承担。AI 不代签、不代「保证」 |
| 2 | **引用必须真实可点开** | `cite_resolve.py` 机械校验：书目/版本号/章节号必须真的存在（见第 6 步） |
| 3 | **高风险动作留人工开关** | 对外发布、投放动作、客户数据处理、合规判断——**一律标为「须人工确认」**，本 skill 不自动执行 |
| 4 | **每个 AI 产出有一次具名人工复核并留记录** | 交付时附 scorecard 原文 + 校验留档；**「未自证项」必须点名**（如分页渲染、无法渲染的版式） |
| 5 | **是否用 AI、用在哪、是否合规——由人判定** | 报告「合规与披露」章写清 AI 参与程度；披露要求由用户/法务决定 |

**必须人工签字的 4 件事**（AI 不得代替）：① 结论是否对外发布 ② 是否构成合规风险（广告法/行业监管） ③ 是否承诺效果 ④ 数据是否含个人信息或客户隐私。

## 工作流（七步，顺序不可跳）

### 1️⃣ 议题树与假设（先出树，再动手）

```bash
python scripts/issue_tree.py --company "企业名" --type b2c    > 议题树.md   # b2c|b2b|platform
python scripts/issue_tree.py --check 议题树.md                             # 不合格不许进下一步
```
- 顶层分支 3±2，**每条分支写成可被证伪的断言**，并写明「什么数据出现它就作废」
- 必须写一句「**是否遗漏**」检查（证明没漏掉一整块）
- 查 `references/03-方法论操作手册.md` §A 章（诊断类）选模型，只留输出物，不堆框架名词

> ⛔ 没有议题树就写正文 = 不合格（BCG 判据）。免检的唯一情形：用户明说「跳过，直接写」。
>
> ⚠️ **反证条件的判据反直觉**：`issue_tree --check` 数的是**命中了几个不同关键词**（`推翻 / 证伪 / 反证 / 若不成立 / 若不 / 如果为假 / 什么数据会否定`），**不是出现次数**。写十遍「本断言作废」也只算 0 个。稳妥做法：正文里同时出现「**反证条件**」「**被推翻**」「**被证伪**」三处不同措辞（实测 3 个即通过）。
> ⚠️ **反证条件必须写在「章标题 → 第一个 `###` 之间」**（同下方第 4 步的章节级判据坑）——写在小点里等于不存在。

### 2️⃣ 事实检索（只查能证伪假设的信息）

- 检索：年报/财报、官网与公开披露、行业报告（艾瑞/易观/QuestMobile 等）、权威媒体、平台榜单
- 每条数据三级标注：**【已核实】**（有来源+日期）/ **【行业认知】**（多来源一致的公知）/ **【未核实】**（单一来源或无法交叉验证）
- **【未核实】不得进交付稿**；关键数字要 ≥2 个独立来源（三角验证）
- 记录**被证伪的假设**（只留成功项 = 不严谨）
- 查不到就写明「查不到 + 因此该结论降级为定性表述」，不许编
- ⚠️ **「来源：」后面别用「公开」开头**：「来源：公开资料」「来源：公开行业观察」会被 `VAGUE_SOURCES` 判成**泛引用硬错误**（本意是拦「来源：网络 / 据了解 / 据称」，但「公开」开头会一起命中）。
  正确写法：把主体写实——「来源：XX 行业观察，单一来源未交叉验证」「来源：公司 2025 年年度报告及媒体整理的公开行情数据」。
  同理，能被 `has_source` 认可的写法只有 4 类：**带 `【已核实】/【行业认知】` 标记 / 含 URL 或《书名》/ 「来源：<可核验主体>」/ 含「本报告测算、年报、统计公报」等可核验主体词**。
- **引用外部文件时**：先把文件读进来，逐条核对版本号/章节号真实存在（第 8 项门禁）

### 3️⃣ 故事线（ghost deck：先写标题，再填内容）

```bash
python scripts/storyline.py --ghost          # 输出骨架模板
python scripts/storyline.py 报告.md          # 标题通读测试
```
- **每个标题必须是完整结论句**（含谓语，尽量含数字）；主题词标题（「市场概览」「现状分析」）一律不许出现
- ⚠️ **数字必须带单位才被承认为「结论句里的数字」**：白名单＝`% ％ 倍 成 个百分点 万元 亿元 万 亿 元 人 家 个 天 月 年 次 点 条 分`。
  **「150 字」「3 类」「3 项」「7 环」都不算**——这类标题会被判成非结论句，直接拖低「结论句占比 ≥80%」。写标题时优先用 `%` / `亿元` / `人` / `个`。
- 自检：把所有标题单独连读，能读成一个完整论证才算过
- 故事线交用户确认后再展开正文

### 4️⃣ 章节填充（**固定骨架由脚本生成**，用户模板优先）

```bash
python scripts/composer.py --out 骨架.md --tier 全量分析    # 全量分析 | 对标研究 | 尽调式拆解
python scripts/composer.py --out 骨架.md --depth 快档       # **深度档：快档 | 标准 | 深度**
python scripts/composer.py --out 骨架.md --tier 全量分析 --with-guide   # 连关键句片段一起写入
python scripts/composer.py --check 报告.md                  # 填完自检：缺节即不合格
```

> ⚠️ **无代码平台（豆包 / ChatGPT 等）跑不了 `composer.py`** → 直接复制 `skeletons/` 下对应的**现成骨架**（3 体裁 × 3 深度共 9 份，如 `骨架-全量分析-标准.md`），**不要让模型自己编结构**——实测过，那会产出很浅。
> ⛔ **骨架的唯一真相在 `scripts/composer.py`（章节与指引在 `scripts/skeleton_data.py`）——不要手搓结构。**
> 体裁（门禁第 2 项）决定用哪套：**全量分析 17 章 / 46 小点 ｜ 对标研究 12 章 / 34 小点 ｜ 尽调式拆解 14 章 / 36 小点**（三种体裁**都是先「三、商业模式解构」再「四、营销手法拆解」**）。
>
> **骨架是两级的：章（##）→ 小点（###）。拆解深度靠小点承载**（只有章节级骨架时，模型对每章只写一段就交差，拆解深度上不来）：
> - 每个小点都带**四件套（照做，别自由发挥）**：① **必含**（逐项打勾，如「①单客收入算式 ②单客成本 ③毛利 ④真杠杆+理由」）｜② **句式**（照填的骨架句，不是"参考"）｜③ **该查的具体节**（细到 §）｜④ **反例**（明确不要写成什么）
> - **`--check` 拦两种病**：① **缺章**（硬判，不合格）② **小点内容过薄**（低于 **120 字**硬下限即不合格）
> - 「小点偏少」只给 **WARN**（提示可再拆细），**不判死**——真实金标准稿里就有若干章是平铺的，判死会误杀
> - 下限的定位：**防「一句话交差」，不是凑字要求**。写得实就不怕量；写不实才会被拦。可用 `--min-chars` 调下限
> - **骨架含两级目录**（章 → 小点，对应交付模板的「手动目录 1 / 2」样式）；成稿后标题会变成结论句，**出稿前跑 `python scripts/composer.py --toc 报告.md` 重生成目录**
> - **深度档直接决定生成时长**（门禁第 5 项「侧重」时问一句）：**快档**＝每章留 2 小点、目标×0.6（约 3,200 字，快）｜**标准**＝默认（约 8,200 字）｜**深度**＝目标×1.25（12,000 字以上）

**骨架摘要**（完整要求见 `composer.py` 输出；用户给了模板时**逐字沿用用户标题**，本骨架只补缺）：

```
执行摘要（五句话：问题/结论/依据/代价/含义，含数字）
一、核心问题：<结论句 + 数字>（governing question，含量化目标与时限）
二、议题树与假设台账（分支 + 反证条件 + 被证伪记录）
三、外部与内部诊断（**PEST** 宏观 → **波特五力** 行业竞争 → **SWOT** 内外因素清单 → **BCG** 业务结构四象限；SWOT 只列事实，推导在「优势与机会」章）
四、商业模式解构（收入结构 / 单位经济 / 盈利模式与价值链位置 / 模式的脆弱点 / **毛利·流水·ROI 拆解**——模式语言与横向对照查 `references/商业模式库/README.md`）
五、营销手法拆解（**STP** 客群与定位 / **4P** 产品与价格·渠道结构·内容·投放·活动 / 私域与复购手法 / **AARRR** 增长链路 / 打法识别 / 流失点——**10 个小点，手法层主战场**）
六、AI 营销专项（逐项：具体怎么操作 + 证据，不写「积极布局 AI」）
七、AI vs 传统运营对比（逐环节表：传统做法 → AI 做法 → 量化差异）
八、洞察（用户没说出口但一听就认的一句话；至少 1 条非消费者来源）
九、三档情景与敏感性（base/upside/downside + 触发条件 + 反事实或区分相关性/因果）
十、优势与机会（**SWOT 的推导层**：SO/WO/ST/WT 与可执行含义；机会要落到「它能做、别人做不了」）
十一、Red Team：这个结论最可能怎么死（≥3 条反方论点 + 回应）
十二、利益相关者与变革阻力（谁会反对、为什么、怎么处理）
十三、合规与披露（个人信息口径、AIGC 参与说明）
十四、取舍：明确不做的事 + 原因
图表清单（见下方「图表清单怎么写」）
附录：数据来源清单（分「可查证」与「未取得一手原文」两类）+ 版本/作者/署名
```

> ⛔ **填完必须跑** `python scripts/composer.py --check 报告.md` —— **缺节即不合格**（固定骨架的意义就在这）。
>
> ⚠️ **骨架给的占位标题不是结论句，必须自己改写**。骨架里形如「1. 关键变量与取值来源（每个变量都要有出处）」「2. 可验证的差异 vs 不可验证的差异（分开写）」的标题是**指引**，直接照抄会被 `storyline` 判非结论句（拖低 C2 的 ≥80%）。**照填句式、但标题要改成含带单位数字的结论句**——例如「1. 关键变量与取值：5 个变量里 4 个来自半年报原始披露」。
> ⚠️ **骨架与判据有一处已知冲突，必须手工规避**：骨架第二章的小点 1 叫「分支 N｜<结论式断言>」，但 `mbb_audit` 的 A2 只读「章标题 → 第一个 `###` 之间」——**把分支写在小点里必判「议题树 0 支」**。规避方式：分支列表放 `## 二、` 章标题正下方（引言），小点里写分支之间的关系与遗漏检查。同理适用于第五章的合规要素（`mbb_audit` E5 读 `## 十二、` 的章引言）与闭环等式（B7 读第三章章引言）。

> ⚠️ **一条最高频的踩坑：章节级判据只看「章标题 → 第一个 `###` 之间」的正文。**
> `composer --check`、`mbb_audit` 的 A2（议题树分支）与 E3（来源清单条目）**都是这个取法**——它们读的是章节自己的 body，而 `###` 子标题会被当作另一个章节切开。
> **后果**：把「分支 1/2/3…」「来源清单表格」写进 `###` 小点里，就会被判成「议题树 0 支」「可查证条目 0 条」（硬错误），而内容其实一直在文件里。
> **正确做法**：
> - **需要被章节级判据识别的结构**（分支列表、来源清单表格、闭环等式）→ 放在 **章标题正下方、第一个 `###` 之前**（作为章节引言）
> - 小点（`###`）只承载"写实"的论述；小点正文仍须满足 ≥120 字
> - 两处都想要 → 引言放结构（可被识别），小点放论述（满足字数），**内容不要只放一处**

**图表清单怎么写（两条判据互相拉扯，必须同时满足）**：

| 写法 | `chart_check`（要求每条带来源与口径，比例 ≥80%） | `source_ledger`（要求含数字句的来源覆盖率 ≥80%） |
|---|---|---|
| **表格** | ❌ 表头行与 `\|---\|` 分隔行也被算进分母，比例最高只能到 5/7 ≈ 71% | ✅ 表格行被跳过，不计入 |
| **列表 + 每句带「。」** | ✅ | ❌ 一条被句号切成 6 句，只有「来源」那半句合格，其余 5 句全判缺来源 |
| **列表 + 单句（推荐）** | ✅ | ✅ |

**推荐写法**：用 `- 图 N｜<结论句标题> — 回答「…」— 来源：<出处> — 口径：<怎么算的> — 图形：charts/chartN.png — 时点：<时点>`
**整条不含句号/分号**（用「—」分隔），这样它是一句话，来源与口径都挂在这一句上，两个脚本都过。
同一条规则适用于「口径声明」等元信息句：写「（来源：本报告方法说明）」，不要留成无来源的裸句子。

**附录「数据来源清单」怎么摆（踩过的坑）**：

- 来源表格**不要放进 `###` 小节**。`mbb_audit` 的 E3 只取「来源清单」章节标题到**第一个子标题**之间的正文——表格放进小节里等于不存在，会判「可查证条目 0 条」**硬错误**。
  正确做法：小节头用加粗段落（`**A1｜可查证来源**`），把表格留在这个章节自己的正文里。
- 附录里**每条来源所在的那一行要带 URL**（或本书名对应的可核验链接），否则 `cite_resolve` 判「待核验」，可解析率上不去（门槛 90%）。
- 引用外部文件时，报告里用的**书名必须与来源清单逐字一致**；正文里不要用简称（写了《XX 报告》，就不要在别处写《报告》），否则 `cite_resolve` 判**幻影书目硬错误**。

> **样式映射（照填样式名即可）**：章节标题 → `Heading 1`；小节 → `Heading 2 / 3`；正文 → `Normal`；图表题注 → `caption`。**不要手设字体字号行距**——模板已固化（`references/18`）。

**每章末尾必须有一句 so-what**（「这意味着…／因此…」）——答不出「所以呢」的内容，删掉。
加分项（默认做）：AIGC 物料样稿（**必须标注 AI 参与**，规范见 `references/16`）+ 数据洞察图表。

### 5️⃣ 压力测试（交付前自攻一轮）

- **Red Team**：≥3 条最强反方论点，逐条给回应
- **反事实与因果**：必须**显式出现「反事实」字样**并落到数字；若确实不可量化，就明写「**这条对照无法量化**——因为 X 未披露」。只写「本报告区分相关性与因果」**不算**（`mbb_audit` B5 认不到，会判缺量化）。建议给 2–3 条反事实对照，其中一条明确写「无法量化」反而是加分项。
- **Pre-mortem**：写下「如果这份报告 6 个月后被证明错了，最可能错在哪一条假设」
- **敏感性**：核心结论给三档，不给区间 = 假精确
- **陷阱自查**：把互动/声量当结果？用相关性冒充因果？洞察其实是常识？
- **口径自查**：同一指标是否有两个值？分母/时钟/时点是否混用？（对应 B7 硬错误）

### 6️⃣ 交付门禁（脚本判定，不允许自评）

```bash
# ⭐ 一条命令跑完全部 7 项判据（把 6–7 次工具往返压成 1 次，约 1 秒）
python scripts/gate.py 报告.md --words 6000 --dep <被引文件路径> --dep-trust auditor

# 要单独调试某一项时（不必全跑）：
python scripts/composer.py --check 报告.md     # 骨架完整性 + 小点不薄
python scripts/mbb_audit.py 报告.md --words 6000 --dep <被引文件路径> --json scorecard.json
```
> ⚠️ **引用了外部文件就必须传 `--dep`**（可多次）。**除了客户/项目文件，还要把「被引用的 skill 参考库」一起传**——若报告里写了「对照 `references/00-打法库.md` 的 §5.8 / §10.5」，那么 `references/00-打法库.md` 就是被引文件，不传它 `cite_resolve` 会把 §5.8 这类章节号判成**幻影引用硬错误**（实测 4 处）。
> 记得把每个 `--dep` 的 sha256 登记进 registry，否则 E6 只能判 ③ 未验证、阻断放行。
> ⚠️ **用 `gate.py` 时 `--dep` 与 `--registry` 必须传绝对路径**。`gate.py` 内部用 subprocess 逐项调用，**cwd 与你的工作目录不同**——传相对路径会找不到 registry，E6 落「③ 未验证」而报「不可放行」，此时报告本身没问题（实测：同一份稿单独跑 `mbb_audit` 判 E6 通过、经 `gate.py` 判未验证，唯一差别就是路径）。
> E6 是**三态**判据，且**①「已验证通过」有硬准入条件**：
> ① **已验证通过** —— dep 可读、引用全部可解析、**且** `--dep-trust auditor`（审计者独立持源）**且** 每个 dep 的 sha256 能在 `assets/dep-registry.json` 里匹配到同名条目
> ② **已验证不通过 = 幻影引用 → 报告硬错误**（打回）——**任何归属下都成立**，引用对不上就是缺陷
> ③ **未验证 → 阻断放行** —— 其余一切情况（未声明归属／哈希未登记／哈希不匹配／路径无效／未传 dep）；属「验证未完成」，**不扣报告分**，但出不了门，且文案必须**指明缺哪一项**
>
> 用法：`python scripts/mbb_audit.py 报告.md --words 6000 --dep <被引文件> --dep-trust auditor --registry assets/dep-registry.json`
> 第 ③ 态的存在是刻意的：**审计者传错路径 ≠ 报告有缺陷**，两者分开显示（`⛔ 硬错误（报告的缺陷）` / `⚠️ 阻断项（验证未完成）`）。
>
> ⚠️ **E6 的能力边界（不许吹）——已从「会被洗白」加固到「只说准话」**：
> · 被审者补交伪造源 → **不再能洗白**（假源哈希不在登记表 → 落 ③ 未验证）
> · **但信任根仍然是人**：谁能敲命令行，谁就能把 `--registry` 指向自己造的表。**限制路径是安全剧场**（有写权限的人照样改表），正确做法是**把「用了哪张表、比了哪个哈希」显示在结论里**，让人能核（E6 输出已强制带 `registry=路径` 与 `sha256=` 前缀）
> · 因此**「已验证通过」仍不等于「引用真实」**——它只等于「与审计者独立持有、且哈希已登记的源一致」。**最终采信要由人签字**（对应「责任边界与签字」第 4 条）
- `mbb_audit` **27 项检查**、5 个维度族（A 结构 25 / B 证据与量化 25 / C 叙事 20 / D 压测 10 / E 治理 20）；其中 **8 项为硬错误**（未核实或未验证数据泄漏 / **口径不自洽 B7** / 主题词标题 / 交付稿缺执行摘要 / 缺可查证来源清单 / 内部过程文档泄漏 / 合规披露缺失 / **引用不可解析 E6**），另设**阻断项**（验证未完成，如 E6 缺 `--dep`）
- **总分 ≥85 且无硬错误**才可交付；交付前把 scorecard 原文贴给用户（**自评不得高于脚本判定**）
- ⚠️ **口径声明（不许含糊）**：85 分线、80% 覆盖率、家族权重、`cite_resolve` 的 90% 可解析率**都是本 skill 自定门槛**，不是外部标准；外部可查证的只有判据本身（MBB 的 SOURCE/action title/假设驱动；Effie 结果占 30%、Cannes 实效类占 50%；IPA 因果四要件）。知识库里另存 75 分门槛与两套权重（`09` §3.1、§3.2），口径不同，**不要混用**
- 然后转 Word（.docx，简体）：**必须以 `assets/交付模板.docx` 为底稿**（页面/正文/标题层级的样式已固化，见 `references/18-交付格式规范.md`）——**只指定样式名，不手搓字体字号**；转换后做 **OOXML 结构加固**（图片段落独立 + 表格 `tblHeader`/`cantSplit` + 图段 `keepLines` + `pPr` 元素顺序合规），并**登记源稿哈希 → 产出哈希**

  ```bash
  # 出稿：以模板为底稿 → 加封面 + 页脚页码 + 剥掉所有注记（客户可直接交付的形态）
  python scripts/md2docx.py 报告.md 报告.docx \
      --title "客户名 营销与商业模式分析报告" --client "客户：XX" \
      --author "交付方：XX" --date 2026-09-23 --footer "XX · 拆解报告"
  python scripts/md2docx.py 报告.docx --verify-only    # 只自检已有产物
  python scripts/deliver_check.py 报告.docx --docx --title "客户名 …"   # 确认「可以直发客户」
  ```
  > ⚠️ **产物自检不通过就退出码非 0，不许交付**。它查两件事：内嵌图形数量是否等于报告里声明的图形数；`word/media/` 里的是不是真图（<1KB 视为空壳）。
  > 为什么必须有这一步：本 skill 曾出现「图形引用扩展名与匹配正则不一致 → 产物 0 张图，而转换脚本静默成功」的事故，**靠肉眼在交付后才发现**（见 `references/04-失败归因总库.md` 附录模式 J）。

- 交付稿内不得混入过程文档

### 7️⃣ 交付后复盘与知识回流（新增，别跳）

> 来源：蓝标等把「复盘」做成固定环节；本 skill 此前交付即结束，是流程缺口。

1. **用户验收清单**：逐条打出用户验收项（格式/字数/结构/匿名）的 ✅/❌，**不拿「脚本全绿」当「可以交了」**
2. **错在哪**：本次出现过的错误逐条记录（幻觉引用？口径错配？时钟混用？），每条写「怎么被发现的」
3. **回流到哪**（照此表更新，别只写不更）：

| 学到的东西 | 回流位置 |
|---|---|
| 新的失败模式 | `references/04-失败归因总库.md` |
| 新的判据（可机械化的） | `scripts/` 里对应脚本 + `smoke_test.py` 加一条断言 |
| 新的行业基准数据 | `references/cases/{行业}.md` |
| 新的外部产品基准 | `references/15-AI营销Agent能力对标.md` |
| 新的物料/合规要求 | `references/16-物料规范与多模态交付.md` |

4. **自检**：改过脚本 → 必须跑 `python scripts/smoke_test.py` 全绿（**65 项**）；改了交付稿 → 必须重跑全套并**登记新哈希**（旧哈希作废要写明原因）

## 基准案例集（golden set）

> 作用：任何人对本 skill 的审计分数有疑问，可跑固定案例集复现。**跑法**：`python scripts/smoke_test.py`（65 项断言，全部用本文件夹内的夹具运行，**不依赖任何外部路径**）

| 案例 | 文件 | 期望 |
|---|---|---|
| 合格样本（基线） | `scripts/fixtures/good_report.md` | 审计 ≥85、无硬错误；`cite_resolve` exit 0 |
| 作弊稿（零分析但会写格式） | `scripts/fixtures/cheat_report.md` | 总分 <60、多项硬错误、**不得放行** |
| 幻影引用稿 | `scripts/fixtures/ghost_cite.md` | `cite_resolve` exit 1、硬错误 = 4 |
| 议题树残缺稿 | `scripts/fixtures/bad_tree.md` / `fake_titles.txt` | 议题树/标题校验拦住 |
| **真实长稿（含外部引用）** | `scripts/fixtures/喵鲜日记-拆解报告.md` + `--dep scripts/fixtures/虚构品牌-商业模式方案.md` | **100/100、0 硬错误**（防误杀的反向验收） |
| **审计者传错 `--dep`** | 同上但 dep 指向无关文件 | **阻断项**（未验证），**不得出现硬错误标签**、不得扣分 |

**一键复现**：`python scripts/smoke_test.py`（65 项断言，全部用**本文件夹内**的夹具运行，不依赖任何外部路径）。golden set 全部存于 `scripts/fixtures/`。

> 注：`fixtures/喵鲜日记-拆解报告.md` 是**真实交付快照**（防误杀用的正向长稿），文中出现的 `outputs/closure_sim.py` 等路径是**当时会话的路径**，**故意不回改**（改了就不是快照了）。它只用于审计回归，**不要执行其中的命令**。

## 脚本清单

| 脚本 | 干什么 | 什么时候跑 |
|---|---|---|
| `scripts/gate.py` | **一次跑完全部 7 项判据**（把 6–7 次工具往返压成 1 次）：议题树/标题/来源/图表/引用/骨架/27 项审计，输出一屏汇总 | 第 6 步（**首选这条**） |
| `scripts/composer.py` | **固定骨架组装器**：按体裁输出报告骨架（每节带必含要素/该查的 references/受哪条判据约束）；`--check` 校验缺节 | 第 4 步出骨架、填完自检 |
| `scripts/skeleton_data.py` | 骨架**数据层**：每节指引四字段（what/size/must/lines）+ 识别关键词 | 改骨架指引时 |
| `scripts/issue_tree.py` | 生成/校验 MECE 议题树与假设台账 | 第 1 步、第 6 步复核 |
| `scripts/storyline.py` | ghost deck 骨架 + 标题通读测试 | 第 3 步、第 6 步 |
| `scripts/source_ledger.py` | 来源覆盖率扫描 + 台账生成 | 第 2 步后、第 6 步 |
| `scripts/chart_check.py` | 图表规范：一图一结论、SOURCE、口径、y 轴原点 | 画图后、第 6 步 |
| `scripts/cite_resolve.py` | **引用可解析性**：书目/版本号/章节号必须真实存在；幻影引用 = 硬错误 | 有外部引用时、第 6 步 |
| `scripts/mbb_audit.py` | 27 项交付审计 + scorecard（含 E6 引用可解析三态，须传 `--dep`；`--cite-min` 调引用门槛） | 第 6 步（唯一放行依据） |
| `scripts/install-hooks.sh` | 把 `scripts/hooks/` 下的钩子装进 `.git/hooks/`（`.git` 不进版本控制 → 新机器 clone 后要跑一次） | 换机器 / 首次接手 |
| `scripts/hooks/pre-push` | **`git push` 前自动把本 skill 镜像同步到 Obsidian 离线存档**（失败不阻塞 push） | 钩子源文件，勿手改 .git/ 里的副本 |
| `scripts/kb_find.py` | **知识库检索入口（用查代替读）**：关键词 → `文件:行`；`--files` 列命中文件；`--sections` 列节索引（含行号） | 需要素材时**先跑它** |
| `scripts/deliver_check.py` | **交付就绪检查**：「能不能直接给客户」——占位符/内部注记/结构齐备（Word 层还查封面与页码） | 出稿后（门禁第 8 项） |
| `scripts/md2docx.py` | **Word 转换 + 封装成交付件**：以 `assets/交付模板.docx` 为底稿，**加封面（标题/客户/交付方/日期）＋ 页脚页码**，**剥掉全部 `<!-- -->` 注记**；OOXML 加固；转换后机械验证内嵌图形数量与真伪，不过关就 exit 1；登记源稿→产出哈希 | 第 6 步最后一段 |
| `scripts/test_sync.py` | **Obsidian 镜像脚本的回归**：临时 vault 里真跑一遍（exit 0／生成存档说明／幂等／**运行前后仓库状态无变化**——防 heredoc 把内容当命令执行） | 改过 `sync-to-obsidian.sh` 或钩子后 |
| `scripts/test_md2docx.py` | md2docx 产物自检的回归（正例通过 / 数量不符被拦 / 空壳可判 / 模板样式被继承）。**依赖 python-docx：缺依赖时显式 SKIP 并 exit 0，不静默通过** | 改过 `md2docx.py` 后 |
| `scripts/smoke_test.py` | 6 支护栏脚本的正负夹具冒烟测试（**65 项断言**，含作弊稿、幻影引用、来源清单自证、版本词形、审计者传错参数等误杀/绕过回归） | 改过脚本后必跑 |
| `scripts/agent_brief.py` | **生成 `AGENT-BRIEF.md`**（给 subagent 的单文件快照：体量/脚本分工/判据/坑，数据现场统计） | 派 subagent 前、架构改动后 |
| `scripts/sync-to-obsidian.sh` | **同步到 Obsidian 离线存档**（rsync 镜像 + 安全校验 + 自动重写存档说明；可挂 git pre-push） | 推送后或定期 |
| `scripts/mbb_common.py` | 共用判据常量（唯一改判据的地方） | — |

## 知识库路由（查，不通读）

> ⭐ **先查，再读**——大文件单份就有 15 万字，**读一节也别整份加载**：
> ```bash
> python scripts/kb_find.py "单位经济"          # 关键词 → 文件:行（只读命中那几行）
> python scripts/kb_find.py "AARRR" --files      # 只列命中文件，先决定读哪份
> python scripts/kb_find.py --sections references/03-方法论操作手册.md   # 该文件的节索引（含行号）
> ```
> 命中后按行号用 Read(offset=行号-5, limit=20) **只读那一段**。

| 需要 | 查 |
|---|---|
| 识别企业用了哪些打法 | `references/00-打法库.md`（116 条，§0 总表） |
| **商业模式模式库（108 + 75 家）** | **`references/商业模式库/README.md`**（先读它当索引，再决定读哪一份；623 条逐条解析 / 28.7 万字 / 六份） |
| 选分析模型 | `references/03-方法论操作手册.md`（A–M 十三类 111 模型，先读章节索引） |
| 行业对标案例 | `references/cases/{行业}.md`（51 档，先读「案例清单」） |
| AI 营销指标体系 | `references/03` §A11（5A/AIPL/FAST/DEEPLINK）+ `references/cases/51` |
| AI 产品营销 | `references/cases/09-AI产品与营销.md` |
| 差距分析与追赶路线 | `references/14-顶级机构差距分析与追赶路线.md` |
| **市面 Agent 能力基准** | `references/15-AI营销Agent能力对标.md`（水位线 + 五条红线 + 可机械化清单） |
| **物料与多模态交付规范** | `references/16-物料规范与多模态交付.md`（渠道物料/文案字数/AIGC 标注/版权台账） |
| 质量标尺 | `references/13-顶级机构对标标准.md`（16 条打分）+ `09` §五自评表 |
| **对标诊断量表（78 条可判定维度）** | `references/17-对标诊断量表-六作业体系.md`（回答「为什么做不到」；是判据的弹药库） |
| 常见失败模式 | `references/04-失败归因总库.md`（附录 AI 执行者模式） |
| **Word 交付排版标准** | `references/18-交付格式规范.md`（底稿 `assets/交付模板.docx`：正文宋体小四／行距 1.5／首行缩进 2 字；H1 黑体 14pt 加粗、H3 楷体 14pt 加粗；表格 10pt 居中） |

## 纪律

1. **卡死处理**：同一错误不重试超过 2 次；校验不过必须上报并给 2–3 个选项，不许死磕、不许静默跳过（跳过要留痕）。
2. **语言**：交付稿一律简体；知识库为简体。
3. **数据红线**：不编数据、不写未核实数据、不写客户隐私；不确定的一律标降级 + 说明。
4. **不承诺**：不承诺效果，不写「保证」「第一」。
5. **不引不存在的东西**：引用任何外部文件前，先确认版本号/章节号真实存在——这是全行业唯一被公开惩罚过的红线。
6. **交付后必须回流**：第 7 步不跳；学到的新判据当天写进脚本与回归。
7. **冻结纪律**：定稿后先清残留 → 跑全套门禁 → 算哈希 → 冻结（此后只读）→ 再转换格式；转换后登记「源稿哈希 → 产出哈希」。

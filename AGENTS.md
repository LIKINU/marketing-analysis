# AGENTS.md · 跨工具接入说明

> **这个文件夹是一套「企业营销与商业模式分析器」，纯 Markdown + Python 写成，不依赖任何特定 AI 工具的语法。**
> 任何能读文件、或能接受粘贴的 Agent 都能用：ChatGPT／Claude／Claude Code／豆包／Cursor／Windsurf／Codex 类工具／本地 Agent／其他。
> 文件夹自带全部依赖（知识库、脚本、回归夹具、模板、哈希登记表），**可以整包搬走，拷到任何机器都能跑**。

---

## 一、这是什么（给 Agent 看的一句话）

给一个企业名 → 产出一份**对标 MBB/4A 的分析报告**（Markdown + Word）：议题树与假设台账、商业模式解构、营销手法拆解、AI 营销专项、Red Team 压测、优势与机会。
**核心是「拆解与解读」，不是「出方案」。** 要出方案请改用同仓的 `Marketing-playbook`。

**先读 `SKILL.md`**，它是唯一入口，里面有完整工作流、判据与路由表。

> ⛔ **开新会话第一步**：先读 `SKILL.md` 的「定位与边界」+「工作流（七步）」+「知识库路由」三节。
> 本文件夹的知识库约 30 万字（含商业模式库 28.7 万字），**通读是最大的浪费** —— 按路由表「查，不通读」。

---

## 二、先记住这一节：强制执行协议 + 门禁

### 2.1 门禁（第一道硬规则）
**第一次使用时，必须先把 8 项问清楚，没问清不准出报告、不准往下走、不准用「一般情况」代替。**
- 8 项清单见 `SKILL.md` 第 0 步（分析对象／体裁／用途与读者／目标字数／侧重／是否要 AIGC 加分项／交付语言／**是否引用外部文件**）
- 问完后**必须产出《任务规则表》请用户确认**，确认前不得产出正文
- **第 8 项（是否引用外部文件）是关键**：若引用了外部文件，**必须拿到文件本身**——本 skill 的 E6 判据要拿报告引用与真实文件逐条对账

### 2.1.5 ⛔ 最高优先级条款（P0 · **优先于下面八条，也优先于 SKILL 的一切默认结构**）

- **P0-1 · 用户给定的结构 > 本 skill 的一切默认骨架** —— 模板／大纲／作业要求／提交清单在场时，一级二级标题**逐字沿用**（不替换／不合并／不拆分／不重排／不自造）；模板没有的内容作为**新增同级／下级章节**补进去。模板写「仅供参考」也默认原样保留。
- **P0-2 · 用户给的素材要「看得见地插进去」，不能溶解** —— 用户给的提纲／数据／案例必须作为**文档中可见章节**出现，保留原名与编号。
- **P0-3 · 「脚本全绿」≠「交付达成」** —— 27 项审计只代表满足本 skill 的默认标准；**用户自定义的验收项（字数／格式／匿名／双格式／附件）必须另行逐条核对并原样输出**。
- **P0-4 · 形状类任务先交 1 页故事线，确认后再写正文**（唯一例外：用户明说「直接给完整版」）；一句话含**两个约束**时 → 动手前**分别复述**两个约束。

### 2.2 八条强制执行协议（**跨平台都适用，违反任何一条＝本次使用失败**）

| # | 协议 | 要点 |
|---|---|---|
| 1 | **启动即立规** | 门禁 8 项一次问全 → 产出《任务规则表》→ 用户确认。**用户此后每次纠偏＝规则表升级**，先复述再动手 |
| 2 | **结构冲突「合并，不取舍」** | 用户要求的结构与本 skill 十三节骨架不一致时，**把骨架映射进去，硬要素一件不丢**；没有对应位的放附录。**禁止为贴合格式砍深度** |
| 3 | **交付前自检单必须原样输出** | 宣告完成前，在回复中逐项输出「用户验收项 + 脚本判定（scorecard 原文）」并标 ✅/❌；任何一项 ❌ → 回去改，不得声称完成 |
| 4 | **工具盘点先于路径** | 要读外部资料，**第一步先盘点已连接的连接器／MCP／API**；连接器可达就**禁止用爬虫硬绕**。涉及付费生成 → 先报预估消耗并征得同意 |
| 5 | **交付形态红线** | 对外交付一律**简体**，Word 走 `assets/交付模板.docx` 底稿（见 `references/18-交付格式规范.md`）；**内部过程文档（议题树、假设台账、审计输出、工作日志）不得出现在交付稿中** |
| 6 | **每轮输出《流程状态》行** | **每轮回复第一行**必须是：`【营销分析 · 流程状态】门禁 8 项：x/8（缺：…）｜任务规则表：{未出/待确认/已确认}｜当前阶段：{1 议题树/2 检索/3 故事线/4 填充/5 压测/6 门禁/7 复盘}`。**缺这一行＝已跑偏** |
| 7 | **跨平台一致性** | **禁止依赖平台专有能力**；能力不足按 §四 对照表降级，**降级不等于跳步**；支持 hook 的平台可选加装（见 §九），**但不得以「没有 hook」为由降低标准** |
| 8 | **卡死处理** | **强制不得变成卡死**。同一项校验连续 2 次不过 → **停止重试**，摊给用户并给 2–3 个选项；脚本报错／环境不可用 → **降级 Markdown 继续产出**；用户说跳过 → 允许，但**必须声明未校验并列出未通过项**。**禁止**：同一错误重试 >2 次／因校验不过就不交付／静默跳过 |

### 2.3 责任边界（谁签字，AI 不代签）

AI 可以写，不能负责。**下列 4 件事必须由人签字**（见 `SKILL.md`「责任边界与签字」）：
① 结论是否对外发布 ② 是否构成合规风险（广告法／行业监管） ③ 是否承诺效果 ④ 数据是否含个人信息或客户隐私。

---

## 三、各平台怎么接入

### A. 有 skill／插件机制的平台
| 平台 | 放哪里 | 怎么触发 |
|---|---|---|
| **Claude Code / Claude.ai** | 专案目录 `.claude/skills/marketing-analysis/` 或用户级 `~/.claude/skills/` | 说「分析 XX 公司」等触发语；`SKILL.md` 的 frontmatter（name/description）会被读取 |
| **WorkBuddy** | 平台的 skills 目录（用户级 `~/.workbuddy/skills/marketing-analysis`，可指向本文件夹） | 直接说企业名 + 分析意图即触发 |
| **Cursor / Windsurf / ZCode 类** | 放在专案根目录，并在专案的 `AGENTS.md` 或规则档写一行「营销分析相关任务请先读 `marketing-analysis/SKILL.md`」 | 依工具的规则加载机制 |
| **其他支持通用 skill 规范的工具** | 依其约定放置；核心是让 Agent 能读到 `SKILL.md` | 同左 |

### B. 有知识库／专案空间的平台
| 平台 | 做法 |
|---|---|
| **ChatGPT Projects** | 把 `SKILL.md` 传进 Project 知识库，自订指令写「处理企业分析时，先读 SKILL.md 并依其流程执行」 |
| **豆包／其他国产 AI** | 上传文件或粘贴内容；若有「知识库／智慧体」功能，同样把 SKILL.md 放进去 |
| **飞书知识库 / Notion** | 导入整个文件夹，需要时让 Agent 读对应页面 |

### C. 什么都没有（纯对话环境）
1. 把 `SKILL.md` **全文粘贴**，说「照这个流程帮我分析这家公司」
2. 需要哪个参考档再贴哪个 —— **本 skill 就是为「按需加载」设计的**，不用一次贴完
3. 常用顺序：`SKILL.md` → `references/03-方法论操作手册.md`（选模型）→ `references/00-打法库.md` → `references/商业模式库/README.md` → `references/cases/{行业}.md`

---

## 四、环境能力对照表（没有某项能力时怎么办）

| 能力 | 有 | 没有 |
|---|---|---|
| **读文件** | 按需读 `references/` 下的文件（先读自解释头 5 行） | 请用户粘贴需要的档 |
| **派出独立 Agent** | 可并行做「多视角审计/红队」——每个 agent 只读事实、不看彼此结论 | **顺序扮演**：每次宣告「现在我是 X 视角」，写完再串 |
| **执行代码** | 跑 8 支脚本（见 §四点五）：议题树/故事线/来源台账/图表规范/引用可解析/27 项审计/回归；Word 用 `assets/交付模板.docx` 出稿 | 退回 Markdown 协议：手工对照清单 + 原样输出自检项 + 附样式对照说明。**降级不等于跳步** |
| **联网检索** | 可核查企业数据与行业事实，做三角验证 | 只用文件内已有内容 + 用户提供材料，并明确标注「未联网核实」 |
| **引用外部文件** | 传 `--dep` 做引用可解析校验（E6） | 手工逐条回源核对「版本号/章节号是否真实存在」——**这是本 skill 的红线，不许省** |
| **写入文件** | 产出后直接落档 | 在对话中输出完整内容，提醒用户自行保存 |

---

## 四之一、质检方法论（**本 skill 的判据体系**）

本 skill 的质检**不靠"再跑一轮"**，而是把每条判据落成脚本：

| 关 | 脚本 | 判什么 | 不过的后果 |
|---|---|---|---|
| 1 | `issue_tree.py --check` | 议题树是否 MECE、假设是否可证伪 | 不许进下一步 |
| 2 | `storyline.py` | 标题是否结论句（禁止主题词标题） | 打回重写 |
| 3 | `source_ledger.py --min 0.8` | 含数字句的来源覆盖率 ≥80% | 打回 |
| 4 | `chart_check.py` | 一图一结论 + SOURCE + 口径 | 打回 |
| 5 | `cite_resolve.py --dep …` | **引用可解析性**（幻影引用 = 硬错误） | 打回 |
| 6 | `mbb_audit.py --words N --dep …` | **27 项审计**（5 维度族；**8 类硬错误** + 阻断项） | 打回 |
| — | `smoke_test.py` | **59 项回归断言**（含作弊稿、幻影引用、误杀回归） | 改过脚本后必跑 |

**核心纪律**：
- **判据只有一种证明方式：删掉它要查的东西，看它会不会叫**（作弊稿必须被拒、真稿不许被误杀）
- **审计器自带反作弊**：作弊稿（零分析但格式齐）会被打到不合格
- **E6 是三态**：①已验证通过 ②幻影引用=硬错误 ③**未验证=阻断**（属"验证未完成"，**不扣报告分但出不了门**）。**审计者传错参数 ≠ 报告有缺陷**
- **自评不得高于脚本判定**：结论只引用脚本输出

---

## 四点五、脚本（**强制层 —— 能跑代码就必须用**）

```bash
# ① 先出议题树（不合格不许进下一步）
python scripts/issue_tree.py --company "企业名" --type b2c > 议题树.md   # b2c|b2b|platform
python scripts/issue_tree.py --check 议题树.md

# ② 故事线（ghost deck）
python scripts/storyline.py --ghost

# ③ 交付门禁（唯一放行依据，逐条跑）
python scripts/storyline.py 报告.md
python scripts/source_ledger.py 报告.md --min 0.8 --out 台账.md
python scripts/chart_check.py 报告.md
python scripts/cite_resolve.py 报告.md --dep <被引文件路径>
python scripts/mbb_audit.py 报告.md --words 6000 --dep <被引文件路径> --json scorecard.json

# ④ 自检（改过任何脚本后必跑）
python scripts/smoke_test.py
```

**Word 出稿**：**必须以 `assets/交付模板.docx` 为底稿**（页面/正文/标题样式已固化），**只指定样式名，不手搓字体字号**；规范见 `references/18-交付格式规范.md`。

**为什么这是真强制**：7 支脚本串成一条链，**拿不到「无硬错误 + 总分 ≥85」的 scorecard 就等于没完成** —— 规则可以假装读过，退出码假装不了。

**没有代码执行能力的平台** → 退回 Markdown 协议（手工对照清单 + 原样输出自检项 + 附样式对照说明）。**降级不等于跳步。**

---

## 五、文件地图

> ⚠️ **唯一权威导航是 `SKILL.md` 的「知识库路由」表** —— 本表只是镜像，两者冲突时以 `SKILL.md` 为准。
> 每个参考档开头都有「自解释头」（这是什么／什么时候读／读完你能／规模），先读那 5 行再决定往不往下读。

| 文件 | 作用 | 什么时候读 |
|---|---|---|
| `SKILL.md` | **唯一入口**：定位与边界／门禁／七步工作流／判据／路由表／纪律 | 永远先读 |
| `scripts/` | **脚本层**：7 支判据脚本 + 夹具（`fixtures/`） | 产出与校验时**必用** |
| `assets/交付模板.docx` | **Word 交付底稿**（样式已固化） | 出 Word 时 |
| `assets/dep-registry.json` | 被引文件哈希登记表（E6 的信任根） | 引用外部文件时 |
| `references/00-打法库.md` | 116 条打法（状况 → 打法） | 识别企业用了哪些打法 |
| `references/03-方法论操作手册.md` | A–M 十三类 111 个分析模型 | 选分析模型时 |
| `references/商业模式库/` | **商业模式模式库**（108+75 家、28.7 万字、六份 + README 索引） | 做商业模式解构章时 |
| `references/cases/` | 52 个行业案例档 | 找同行对标时（只读你那一档） |
| `references/13` / `14` / `17` | 质量标尺 / 差距分析与追赶路线 / 对标诊断量表（78 条维度） | 对标与自检时 |
| `references/18-交付格式规范.md` | Word 排版标准 | 出 Word 时 |
| `references/04-失败归因总库.md` | 失败模式库 | 风险自检 |
| `references/范例/` | 样张 | 想看成品长什么样时 |
| `AGENT-BRIEF.md` | 给 agent 的单文件快照（自动生成） | 派 subagent 时先给它 |

---

## 六、语言约定

- **全仓库统一简体中文**：`SKILL.md`／本档／`references/`／脚本注释，一律简体。
- **交付稿一律简体**（硬门槛）；统一语言后这条的意义是「**挡住外部粘贴进来的繁体**」。
- 若用户要求其他语言，以用户当次指示为准。

---

## 七、怎么让它越用越准

项目结束后，把新学到的东西写回对应文件（**第 7 步「交付后复盘与知识回流」不跳**）：

| 新学到什么 | 写到哪 |
|---|---|
| 新的失败模式 | `references/04-失败归因总库.md` |
| 新的判据（可机械化的） | `scripts/` 对应脚本 + `smoke_test.py` 加断言 |
| 新的行业基准数据 | `references/cases/{行业}.md` |
| 新的外部产品基准 | `references/15-AI营销Agent能力对标.md` |
| 新的物料/合规要求 | `references/16-物料规范与多模态交付.md` |
| 新的商业模式/模式体系 | `references/商业模式库/` |

**改过脚本 → 必须跑 `smoke_test.py` 全绿**；改了交付稿 → 重跑全套 + **登记新哈希**（旧哈希作废要写明原因）。

**不写进来的**：公司隐私、未核实的数据、客户机密。

---

## 八、验收：一份合格的分析报告长什么样

- **格式**：Markdown + `.docx`（Word 按 `assets/交付模板.docx` 的样式）
- **篇幅**：以《任务规则表》确认的**目标字数**为准（未指定时预设 6,000–10,000 字）
- **结构**：执行摘要 + 十三节骨架 + 图表清单 + 附录（数据来源清单，分「可查证」与「未取得一手原文」两类）
- **必含**：可证伪的议题树、三级可信度标注、三档情景与敏感性、Red Team ≥3 条反方论点、每章一句 so-what
- **不合格的信号**：主题词标题（「市场概览」）／未核实数据进正文／口径前后不一致（同一指标两个值）／引用对不上源／把互动声量当结果

---

## 九、可选：在支持 hook 的平台加装硬约束

> **前提**：协议 1–8 是**跨平台通用**的约束。若平台支持 hooks（**Claude Code 与 WorkBuddy 同构**），可再加一层硬约束 —— **这是可选增强，不是必需**。

### 9.1 支持情况
| 平台 | hook 支持 | 配置位置 |
|---|---|---|
| WorkBuddy | ✅（与 Claude Code 同构） | `~/.workbuddy/settings.json` 的 `hooks` 字段 |
| Claude Code | ✅ | `~/.claude/settings.json` |
| ChatGPT／豆包／其他 | ❌ | 靠协议 1–8 ＋ 用户检查《流程状态》行 |

### 9.2 最值得配的一个：Stop（强制输出交付自检）
AI 想结束回答时，检查是否已输出「用户验收项 + scorecard」；没输出就拦住、逼它补齐。

```json
{
  "hooks": {
    "Stop": [
      { "hooks": [ { "type": "prompt",
        "prompt": "判断本轮对话是否属于「企业营销/商业模式分析」任务（关键词：分析某公司／拆解营销／商业模式分析／尽调式拆解／对标顶级咨询，或使用了 marketing-analysis skill）。\n\n若【不是】→ 返回 {\"ok\": true}\n\n若是 → 检查最后一条回复是否已输出：①《流程状态》行 ②用户验收项的逐条 ✅/❌ ③脚本判定（mbb_audit 的 scorecard 结论，含「总分」与「硬错误/阻断项」）。三项缺任一项 → {\"ok\": false, \"reason\": \"这是分析任务，但还没输出《流程状态》行／验收项／脚本判定。请补齐；若审计有硬错误或阻断项，先修正再声称完成。\"}",
        "continueOnBlock": true, "timeout": 30 } ] }
    ]
  }
}
```

### 9.3 可选再加：UserPromptSubmit（开头注入协议）
让用户不必每次说「先出门禁」：

```json
{
  "hooks": {
    "UserPromptSubmit": [
      { "hooks": [ { "type": "prompt",
        "prompt": "判断用户这条输入是否涉及「企业分析／拆解营销／商业模式分析」。\n\n不是 → {\"ok\": true}\n\n是 → {\"ok\": false, \"reason\": \"【marketing-analysis 强制协议】这是分析任务。请先读该 skill 的 SKILL.md，然后：① 执行门禁，8 项一次问全（含第 8 项「是否引用外部文件」，若是则索取文件）；② 输出《任务规则表》等用户确认；③ 确认前不得产出正文；④ 每轮回复以《流程状态》行开头；⑤ 交付前跑 7 支脚本并原样输出 scorecard 结论。\"}",
        "continueOnBlock": true, "timeout": 30 } ] }
    ]
  }
}
```

### 9.4 注意
- hook 是**系统级配置**，对所有专案生效 → 判断条件务必加「只在分析任务时触发」。
- 每次触发多一次 LLM 判断（有消耗）。**建议先只配 Stop**，观察一周再加 UserPromptSubmit。
- 本机 `~/.workbuddy/settings.json` **当前没有 `hooks` 段**（上述为配置模板，未安装）。
- 改完 settings.json 后，部分平台需在 `/hooks` 面板确认变更才生效。
